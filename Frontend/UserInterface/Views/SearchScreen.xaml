<UserControl x:Class="UserInterface.Views.SearchScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserInterface.Views"
             xmlns:hc="clr-namespace:UserInterface.HelperControls"
             xmlns:conv="clr-namespace:UserInterface.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <hc:ResizableCentredTextBox Text="Type in the name of a molecule and press Enter." />
        <TextBox x:Name="textBox" Grid.Row="1" Margin="10 10 10 10" FontSize="15" PreviewKeyUp="TextBox_PreviewKeyUp" />
        <hc:ResizableCentredTextBox Grid.Row="2" Text="{Binding ErrorMessage}" Visibility="{Binding IsError, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
</UserControl>
