<UserControl x:Class="UserInterface.Views.SearchResultsScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserInterface.Views"
             xmlns:hc="clr-namespace:UserInterface.HelperControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <hc:ResizableCentredTextBox Grid.ColumnSpan="2" Text="Search results:" />
        <ComboBox Grid.Row="1" Grid.ColumnSpan="2" x:Name="comboBox" Margin="10 10 10 10" ItemsSource="{Binding SearchResults}" SelectedItem="{Binding SelectedMolecule}" />
        <Button Grid.Row="3" Margin="10 10 10 10" Command="{Binding BackCommand}">
            <hc:ResizableCentredTextBox Text="Search again" />
        </Button>
        <Button Grid.Row="4" Grid.ColumnSpan="2" Margin="10 10 10 10" Command="{Binding SubmitCommand}" CommandParameter="{Binding SelectedMolecule}">
            <hc:ResizableCentredTextBox Text="Map selected molecule" />
        </Button>
    </Grid>
</UserControl>
